/* Token público de Mapbox. En Netlify lo GENERA build.sh desde la variable de
   entorno MAPBOX_TOKEN en cada deploy — no edites este archivo a mano.
   Este valor por defecto (vacío) hace que la cotización instantánea quede
   oculta si el sitio se abre sin build (ej. arrastrando la carpeta). */
window.MAPBOX_TOKEN = "";
