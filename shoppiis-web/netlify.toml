# Configuración de Netlify (opcional). Si arrastrás la carpeta a netlify.com/drop
# no hace falta tocar nada; esto sirve si conectás un repo de Git.

[build]
  publish = "."
  # Genera config.js con el token de Mapbox desde la variable de entorno
  # MAPBOX_TOKEN (configurada en Netlify → Site settings → Environment variables).
  command = "bash build.sh"

# Cabeceras de seguridad básicas
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache largo para imágenes (se sirven más rápido)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
